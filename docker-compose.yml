version: "3.8"

services:
  minikb:
    build: .
    container_name: minikb
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./.env:/app/.env:ro
    restart: unless-stopped
